<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F8F9FA">

    <!-- Header Section with Demo Button -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#0F2445"
        android:padding="16dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Customer List"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="#FFFFFF"/>
        
        <!-- यह बटन सिर्फ तेरी टेस्टिंग के लिए है, बाद में हटा देंगे -->
        <Button
            android:id="@+id/btnAddDemo"
            android:layout_width="wrap_content"
            android:layout_height="35dp"
            android:text="+ Add Demo"
            android:textSize="12sp"
            android:backgroundTint="#4CAF50"
            android:textColor="#FFFFFF"/>
    </LinearLayout>

    <!-- Empty List Message -->
    <TextView
        android:id="@+id/tvEmptyMessage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="No customers found.\nList is empty."
        android:gravity="center"
        android:textSize="18sp"
        android:textColor="#888888"
        android:visibility="visible"/> <!-- डिफ़ॉल्ट रूप से दिखेगा -->

    <!-- Dynamic List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCustomers"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"
        android:visibility="gone"/>

</LinearLayout>
