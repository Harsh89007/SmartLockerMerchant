<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="Smart Locker Merchant"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

        <!-- Activities -->
        <activity android:name=".SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SignUpActivity" android:exported="false" />
        <activity android:name=".LoginActivity" android:exported="false" />
        <activity android:name=".SetMpinActivity" android:exported="false" />
        <activity android:name=".EnterMpinActivity" android:exported="false" />
        <activity android:name=".MerchantDashboardActivity" android:exported="false" />
        <activity android:name=".AddCustomerActivity" android:exported="false" />
        <activity android:name=".CustomerListActivity" android:exported="false" />
        <activity android:name=".CustomerDetailControlActivity" android:exported="false" />
        <activity android:name=".FrpAccountsActivity" android:exported="false" />

    </application>

</manifest>
